each value, key in theme.footer
     - var re = /^(http|https):\/\/*/i;
     - var absolute = re.test(value)
     - var tar = absolute ? '_blank' : '_self'
     - var url = absolute ? value : url_for(value)
     a(href=url, target=tar)
         != key
